apiVersion: v1
kind: Service
metadata:
  name: {{ include "Chabloom.Accounts.Frontend.fullname" . }}
  labels:
    {{- include "Chabloom.Accounts.Frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Chabloom.Accounts.Frontend.selectorLabels" . | nindent 4 }}
